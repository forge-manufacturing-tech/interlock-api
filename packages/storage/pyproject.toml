[project]
name = "storage"
version = "0.1.0"
description = "Abstract storage provider for Interlock"
readme = "README.md"
authors = []
requires-python = ">=3.12"
dependencies = [
    "google-cloud-storage",
]

[build-system]
requires = ["uv_build>=0.9.22,<0.10.0"]
build-backend = "uv_build"
