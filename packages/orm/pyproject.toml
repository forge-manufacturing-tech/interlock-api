[project]
name = "orm"
version = "0.1.0"
description = "CRUD repository for the Interlock manufacturing graph"
readme = "README.md"
authors = []
requires-python = ">=3.12"
dependencies = [
    "database",
    "models",
]

[build-system]
requires = ["uv_build>=0.9.22,<0.10.0"]
build-backend = "uv_build"

[tool.uv.sources]
database = { workspace = true }
models = { workspace = true }
