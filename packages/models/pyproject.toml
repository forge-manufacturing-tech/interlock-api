[project]
name = "models"
version = "0.1.0"
description = "Pydantic models for the Interlock manufacturing graph"
readme = "README.md"
authors = [
    { name = "Nathan Alam", email = "nathanalam99@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2.0.0",
    "sqlmodel"
]

[build-system]
requires = ["uv_build>=0.9.22,<0.10.0"]
build-backend = "uv_build"
