[project]
name = "database"
version = "0.1.0"
description = "SQLite database manager for Interlock API"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
