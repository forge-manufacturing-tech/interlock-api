[project]
name = "database"
version = "0.1.0"
description = "Graph database manager using Kùzu for Interlock API"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "kuzu>=0.4.2",
    "pydantic>=2.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
