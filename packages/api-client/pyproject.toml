[project]
name = "api-client"
version = "0.1.0"
description = "Generated API client for Interlock"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.27.0",
    "attrs>=23.2.0",
    "python-dateutil>=2.8.2",
]

[build-system]
requires = ["uv_build>=0.9.22,<0.10.0"]
build-backend = "uv_build"
